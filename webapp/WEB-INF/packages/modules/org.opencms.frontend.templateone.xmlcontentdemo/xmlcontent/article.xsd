
<xsd:schema xmlns:xsd="http://www.w3.org/2001/XMLSchema" elementFormDefault="qualified">
	
	<xsd:include schemaLocation="opencms://opencms-xmlcontent.xsd"/>	
	<xsd:element name="Articles" type="OpenCmsArticles"/>
	
	<xsd:complexType name="OpenCmsArticles">
		<xsd:sequence>
			<xsd:element name="Article" type="OpenCmsArticle" minOccurs="0" maxOccurs="unbounded"/>
		</xsd:sequence>
	</xsd:complexType>

	<xsd:complexType name="OpenCmsArticle">
		<xsd:sequence>
			<xsd:element name="Title" type="OpenCmsString" />
			<xsd:element name="Teaser" type="OpenCmsString" maxOccurs="5"/>
			<xsd:element name="Text" type="OpenCmsHtml" />
			<xsd:element name="Release" type="OpenCmsDateTime" />			
			<xsd:element name="Author" type="OpenCmsString" />
		</xsd:sequence>
		<xsd:attribute name="language" type="OpenCmsLocale" use="required"/>
	</xsd:complexType>

	<xsd:annotation>
		<xsd:appinfo>
			<resourcebundle name="org.opencms.frontend.templateone.xmlcontentdemo.workplace"/>
			<mappings>
				<mapping element="Title" mapto="property:Title" />
				<mapping element="Release" mapto="attribute:datereleased" />
			</mappings>
			<validationrules>
				<rule element="Title" regex="!.*[Ee]rror.*" message="${key.editor.errormessage.Article.mustnotcontainerror.1} ${validation.path} ${key.editor.errormessage.Article.mustnotcontainerror.2}" />
				<rule element="Title" regex="!.*[Ww]arn.*" type="warning" message="${key.editor.warning.Article.mustnotcontainwarn.1} ${validation.path} ${key.editor.warning.Article.mustnotcontainwarn.2}"/>
				<rule element="Teaser" regex="!.*[Ww]arn.*" type="warning" message="${key.editor.warning.Article.mustnotcontainwarn.1} ${validation.path} ${key.editor.warning.Article.mustnotcontainwarn.2}"/>
				<rule element="Text" regex="!.*[Ww]arn.*" type="warning" message="${key.editor.warning.Article.mustnotcontainwarn.1} ${validation.path} ${key.editor.warning.Article.mustnotcontainwarn.2}"/>
			</validationrules>
			<defaults>
				<default element="Title" value="${key.editor.defaults.Article.Title}"/>
				<default element="Teaser" value="${key.editor.defaults.Article.Teaser}"/>
				<default eleemnt="Release" value="${currenttime}"/>
				<default element="Author" value="${currentuser.fullname}"/>
			</defaults>
		</xsd:appinfo>

	</xsd:annotation>  
</xsd:schema>