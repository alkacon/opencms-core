<%@ page import="org.opencms.jsp.*,org.opencms.frontend.templateone.form.*" %><%
	
	CmsJspActionElement cms = new CmsJspActionElement(pageContext, request, response);
	
	CmsCaptchaSettings settings = new CmsCaptchaSettings(cms);
	CmsCaptchaField captcha = new CmsCaptchaField(settings, null, null);
	captcha.writeCaptchaImage(cms);

%>